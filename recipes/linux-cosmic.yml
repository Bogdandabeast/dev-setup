# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

# Información general de la imagen
name: cosmic
description: Imagen personalizada con escritorio COSMIC, Visual Studio Code y configuración para gráficas AMD.

# Base de la imagen y versión
base-image: ghcr.io/ublue-os/cosmic-atomic-main
image-version: latest

# Configuración de módulos
modules:
  - type: rpm-ostree
    repos:
      - https://copr.fedorainfracloud.org/coprs/bieszczaders/kernel-cachyos/repo/fedora-%OS_VERSION%/bieszczaders-kernel-cachyos-fedora-%OS_VERSION%.repo
    install:
      - kernel-cachyos-rt
      - kernel-cachyos-rt-devel-matched

  - type: script
    snippets:
      - "echo 'Habilitando SELinux' && setsebool -P domain_kernel_load_modules on"
      - "echo 'Aplicando override del kernel' && rpm-ostree override remove kernel kernel-core kernel-modules kernel-modules-core kernel-modules-extra --install kernel-cachyos-rt"

  - type: files
    files:
      - source: system
        destination: /

  - type: rpm-ostree
    repos:
      - https://pkg.cloudflareclient.com/cloudflare-warp-ascii.repo
      - vstudio.repo
      - https://copr.fedorainfracloud.org/coprs/che/zed/repo/fedora-42/che-zed-fedora-42.repo
    install:
      - cloudflare-warp
      - deluge
      - code
      - firefox
      - zed
      # Virt-manager y dependencias
      - virt-manager
      - libvirt-daemon-driver-network
      - libvirt-daemon-driver-nodedev 
      - libvirt-daemon-driver-qemu 
      - libvirt-daemon-driver-storage-core 
      - qemu-audio-spice 
      - qemu-char-spice 
      - qemu-device-display-qxl 
      - qemu-device-display-virtio-gpu 
      - qemu-device-display-virtio-vga 
      - qemu-device-usb-redirect
      - qemu-system-x86-core
      # CLI utilidades
      - fastfetch
      - fish
      - gh
      - bat

  - type: default-flatpaks
    notify: true
    system:
      install:
        - com.valvesoftware.Steam
        - com.heroicgameslauncher.hgl
        - com.valvesoftware.Steam.CompatibilityTool.Proton-GE
        - org.gnome.Loupe
        - io.github.shiftey.Desktop
        - io.github.dvlv.boxbuddyrs
        - io.missioncenter.MissionCenter
        - com.mattjakeman.ExtensionManager
        - org.keepassxc.KeePassXC
        - ru.linux_gaming.PortProton
        - org.gnome.Boxes
    user: {}

  - type: signing

alt-tags:
  - cachyos-latest







